# Copy to .env and adjust as needed. Used by docker-compose to configure the
# Postgres container, and by the server (server/Properties/launchSettings.json)
# to connect to it.
# 5433 avoids clashing with a locally-installed Postgres already bound to 5432.
DB_HOST=localhost
DB_PORT=5433
DB_NAME=brydon
DB_USER=brydon
DB_PASSWORD=brydon

# Optional: credentials for the demo user seeded on first run when the users
# table is empty. Falls back to demo/password123 if unset.
DEMO_USERNAME=demo
DEMO_PASSWORD=password123
